{
  "name": "labs",
  "module": "./src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.1.0",
    "vite": "^6.3.4",
    "wrangler": "^4.14.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "wrangler types && vite dev",
    "build": "wrangler types && vite build",
    "preview": "wrangler types && vite build && vite preview",
    "deploy": "wrangler types && vite build && wrangler deploy"
  }
}